{{- $secretName := include "opensearch.transport-cert-secret-name" . }}
{{- $install := and (eq (include "opensearch.tlsEnabled" .) "true") .Values.global.tls.enabled (eq (include "opensearch.transportCertificatesSpecified" .) "true") }}
{{- if and $install (ne (include "certProvider" .) "cert-manager") (not (lookup "v1" "Secret" .Release.Namespace $secretName)) }}
apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/resource-policy": keep
data:
{{- if not .Values.global.tls.generateCerts.enabled }}
  {{- if .Values.opensearch.tls.transport.certificates.crt }}
  tls.crt: {{ .Values.opensearch.tls.transport.certificates.crt | quote }}
  {{- end }}
  {{- if .Values.opensearch.tls.transport.certificates.key }}
  tls.key: {{ .Values.opensearch.tls.transport.certificates.key | quote }}
  {{- end }}
  {{- if .Values.opensearch.tls.transport.certificates.ca }}
  ca.crt: {{ .Values.opensearch.tls.transport.certificates.ca | quote }}
  {{- end }}
{{- end }}
{{- end }}