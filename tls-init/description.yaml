build:
  networkRules: nc.product.dtrust
  env:
    type: microservice
    version: generic
publication:
  docker:
    - latest
    - timestamp
deploy:
  openshift:
    service: "opensearch-tls-init"
    templatePatching: disabled