build:
  networkRules: nc.product.dtrust
  env:
    type: microservice
    version: generic-1.0
publication:
  docker:
    - latest
    - timestamp
  files:
    INFRASTRUCTURE1.0:
      - target/*.zip
    STANDALONE_ZIP1.0:
      - mask: scripts/*.zip
        latest: true
deploy:
  dmp:
    service: opensearch-service
    version: 1.12