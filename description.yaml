build:
  networkRules: nc.product.dtrust
  env:
    type: microservice
    version: python-3.8.6
    tree: tree-7
publication:
  docker:
    - latest
    - timestamp
  files:
    INFRASTRUCTURE1.0:
      - target/*.zip
deploy:
  dmp:
    service: opensearch-service
    version: 1.12