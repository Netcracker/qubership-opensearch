build:
  networkRules: nc.product.dtrust
  env:
    type: microservice
    version: generic-1.0
publication:
  docker:
    - latest
    - timestamp
  files:
    INFRASTRUCTURE1.0:
      - target/*.zip
deploy:
  dmp:
    service: opensearch-service
    version: 1.12